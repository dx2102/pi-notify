{
  "name": "pi-notify",
  "version": "0.1.0",
  "type": "module",
  "description": "Send delayed notifications to a pi session via Unix socket",
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  },
  "pi": {
    "extensions": [
      "./index.js"
    ]
  }
}
